extraction:
  cpp:
    before_index:
      - git clone https://github.com/ANYbotics/kindr.git && \
            cd kindr && \
            git checkout 1.1.3 && \
            mkdir build && \
            cd build && \
            cmake .. && \
            sudo make install
